import type { CanUse } from "./types";

/**
 * TEMPORARY STUB of canUse (Phase 2 Step 0): everything is allowed until A9
 * adds plans and quotas with the same type. Switching is a one-line import change.
 */
export const canUse: CanUse = async () => true;
