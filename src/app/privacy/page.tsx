import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";

// Public (PUBLIC_PREFIXES in src/proxy.ts): plain-language privacy page (C6).
// Every sentence must stay true to the code; when a feature changes what is
// stored or who sees it, change this page in the same PR.

export const metadata: Metadata = { title: "حریم خصوصی | غلتک" };

const UPDATED = "۴ مهر ۱۴۰۵";

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <section className="flex flex-col gap-3">
      <h2 className="text-lg font-semibold">{title}</h2>
      <div className="flex flex-col gap-3 leading-8 text-neutral-800">{children}</div>
    </section>
  );
}

function List({ items }: { items: React.ReactNode[] }) {
  return (
    <ul className="flex list-disc flex-col gap-1 ps-6 marker:text-neutral-400">
      {items.map((item, i) => (
        <li key={i}>{item}</li>
      ))}
    </ul>
  );
}

export default function PrivacyPage() {
  return (
    <div className="flex flex-1 flex-col">
      <header className="border-b border-neutral-200">
        <div className="mx-auto flex max-w-3xl items-center justify-between gap-4 px-4 py-3">
          <Link href="/" className="flex items-center gap-2">
            <Image src="/brand/logo-symbol.png" alt="" width={32} height={32} />
            <span className="font-bold">غلتک</span>
          </Link>
        </div>
      </header>

      <main className="mx-auto flex w-full max-w-3xl flex-1 flex-col gap-8 px-4 py-6 md:py-10">
        <div className="flex flex-col gap-2">
          <h1 className="text-2xl font-bold">حریم خصوصی</h1>
          <p className="text-neutral-600">
            به زبان ساده: غلتک چه داده‌ای نگه می‌دارد، برای چه، چه کسی آن را می‌بیند و کجا. آخرین به‌روزرسانی: {UPDATED}.
          </p>
        </div>

        <Section title="چه داده‌ای نگه می‌داریم">
          <p>
            <strong>دربارهٔ فروشنده:</strong>
          </p>
          <List
            items={[
              "شمارهٔ موبایلی که با آن وارد می‌شوید.",
              "اطلاعات فروشگاه: نام، لوگو و راه‌های تماسی که خودتان وارد می‌کنید.",
              "شمارهٔ کارت و شبا برای کارت‌به‌کارت، و اطلاعات درگاه پرداختتان. این‌ها رمزشده ذخیره می‌شوند.",
              "محصولات، موجودی و تغییرات آن، لینک‌های خرید، و اشتراکتان.",
            ]}
          />
          <p>
            <strong>دربارهٔ مشتری‌های فروشنده:</strong>
          </p>
          <List
            items={[
              "نام، موبایل و آدرسی که مشتری هنگام خرید می‌نویسد یا فروشنده برایش ثبت می‌کند.",
              "سفارش‌ها، پرداخت و ارسال آن‌ها، و تصویر رسید کارت‌به‌کارت اگر مشتری فرستاده باشد.",
              "اگر مشتری آنلاین پرداخت کند: کد پیگیری درگاه و شمارهٔ کارت پوشیده‌ای که درگاه برمی‌گرداند (فقط چند رقم اول و آخر). شمارهٔ کامل کارت مشتری هرگز به غلتک نمی‌رسد.",
              "پیامک‌هایی که برای سفارش فرستاده شده: شمارهٔ گیرنده، نوع پیامک و وضعیت ارسال.",
            ]}
          />
          <p>
            <strong>فنی:</strong>
          </p>
          <List
            items={[
              "کد ورود پیامکی فقط به‌صورت درهم‌شده (hash) ذخیره می‌شود و بعد از ۲ دقیقه باطل است.",
              "یک کوکی ورود برای فروشنده. مشتری‌ها کوکی ندارند و حساب کاربری لازم ندارند.",
              "برای لینک‌های خرید فقط تعداد بازدید هر روز شمرده می‌شود، بدون آدرس IP.",
              "برای جلوگیری از سوءاستفاده، تعداد سفارش هر آدرس IP مدتی کوتاه در حافظهٔ سرور شمرده می‌شود و در دیتابیس ذخیره نمی‌شود.",
            ]}
          />
        </Section>

        <Section title="برای چه">
          <p>
            فقط برای اینکه غلتک کار کند: ثبت و پیگیری سفارش، کنترل موجودی، پرداخت، ارسال، پیامک به مشتری و گزارش فروش خود
            فروشنده. داده‌ها را نمی‌فروشیم، برای تبلیغات به کار نمی‌بریم، و هیچ ابزار ردیابی یا آمارگیری بیرونی در غلتک نیست.
          </p>
        </Section>

        <Section title="چه کسی می‌بیند">
          <List
            items={[
              "داده‌های هر فروشگاه را فقط همان فروشگاه می‌بیند. هیچ فروشنده‌ای دادهٔ فروشگاه دیگری را نمی‌بیند.",
              "مشتری فقط صفحهٔ سفارش خودش را می‌بیند، با لینکی که فقط او دارد. شمارهٔ کارت فروشنده فقط روی همین صفحه و فقط تا وقتی سفارش منتظر پرداخت است نشان داده می‌شود.",
              "رسید کارت‌به‌کارت فقط برای فروشندهٔ همان سفارش است و هیچ‌وقت عمومی نمی‌شود.",
              "تیم غلتک فقط وقتی به داده‌ها نگاه می‌کند که برای رفع خطا یا پشتیبانی لازم باشد.",
            ]}
          />
          <p>
            <strong>سرویس‌هایی که بخشی از داده به آن‌ها می‌رسد:</strong> کاوه‌نگار برای ارسال پیامک (شمارهٔ گیرنده و متن
            پیامک)، و اگر فروشنده درگاه وصل کرده باشد، درگاه پرداخت او (مثلاً زرین‌پال) برای پرداخت آنلاین. هر دو داخل
            ایران‌اند.
          </p>
        </Section>

        <Section title="کجا ذخیره می‌شود">
          <List
            items={[
              "روی سرورهای داخل ایران. غلتک در زمان اجرا به هیچ سرویس خارج از ایران وابسته نیست.",
              "فایل‌ها (عکس محصول، لوگو و رسید) در فضای ذخیره‌سازی خصوصی. رسیدها فقط از راه حساب فروشنده باز می‌شوند.",
              "شمارهٔ کارت، شبا و اطلاعات درگاه با کلیدی رمز می‌شوند که جدا از دیتابیس نگه داشته می‌شود.",
              "از دیتابیس و فایل‌ها هر روز پشتیبان رمزشده گرفته می‌شود، در جایی جدا از خود سرور.",
            ]}
          />
        </Section>

        <Section title="خروجی گرفتن و حذف">
          <List
            items={[
              <>
                <strong>خروجی:</strong> مالک فروشگاه هر وقت بخواهد از «تنظیمات» ← «خروجی داده» فایل محصولات، مشتریان و
                سفارش‌ها را می‌گیرد. شمارهٔ کارت، شبا و اطلاعات درگاه در این فایل‌ها نمی‌آید.
              </>,
              <>
                <strong>حذف حساب فروشنده:</strong> درخواستتان را به پشتیبانی غلتک بدهید. حساب و همهٔ داده‌های فروشگاه،
                از جمله داده‌های مشتری‌هایش، پاک می‌شود. نسخه‌های پشتیبان قدیمی حداکثر ۳۰ روز بعد خودبه‌خود پاک می‌شوند.
                فقط شمارهٔ موبایل و تاریخ دورهٔ رایگان می‌ماند، تا یک شماره دوباره دورهٔ رایگان نگیرد. پیشنهاد می‌کنیم پیش از حذف خروجی بگیرید.
              </>,
              <>
                <strong>مشتری‌ها:</strong> برای دیدن، اصلاح یا حذف اطلاعاتتان به فروشنده‌ای بگویید که از او خرید کرده‌اید؛
                اطلاعات مشتری‌های هر فروشگاه دست خود آن فروشگاه است و غلتک در انجام درخواست کمکش می‌کند.
              </>,
            ]}
          />
        </Section>
      </main>
    </div>
  );
}
