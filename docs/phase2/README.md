# فاز ۲: راه‌اندازی واقعی، پرداخت آنلاین و تثبیت

**وضعیت:** پیشنهاد، در انتظار تأیید تیم.

**مبنا:**
- سند محصول «پلتفرم مدیریت فروشندگان آنلاین»، بخش‌های ۹ (پرداخت)، ۱۰ (ارسال)، ۱۱ (گزارش)، ۱۴ و ۱۵ (مدل درآمد)، ۱۶ و ۱۷ (نسخهٔ اولیه و ورود به بازار)، ۲۰ (ریسک‌ها) و ۲۱ (مسیر توسعه).
- مستندات فاز ۱ (`docs/phase1/`): فهرست «خارج از دامنه» و محدودیت‌هایی که در وضعیت هر تسک ثبت شده است.

ابتدا همین فایل را بخوانید، بعد فایل Track خودتان را:
- [Track A: پلتفرم و حساب فروشنده](./TRACK-A.md)
- [Track B: پرداخت آنلاین و ارتباط با مشتری](./TRACK-B.md)

---

## فاز ۲ در نقشهٔ راه سند محصول کجاست

| مرحله در سند (بخش ۲۱) | در پروژهٔ ما |
|---|---|
| ۱. اعتبارسنجی | پیش از فاز ۱ |
| ۲. نسخهٔ اولیه: هستهٔ محصول و آزمایش با حدود ۱۰ فروشگاه | **فاز ۱، انجام شد** (A1 تا A5، B1 تا B5) |
| ۳. تثبیت: بهبود سفارش، موجودی، مشتری، پرداخت و ارسال بر اساس رفتار کاربران واقعی | **فاز ۲ (همین سند)** |
| ۴. اتوماسیون: پیام‌رسان‌ها، پاسخ خودکار، بازاریابی و هوش مصنوعی | فاز ۳ |
| ۵. رشد، ۶. بازار خارجی | بعد از فاز ۳ |

## هدف فاز ۲
1. **راه‌اندازی واقعی:** برنامه روی سرور داخل ایران اجرا شود و **حدود ۱۰ فروشگاه فعال** هر روز از آن استفاده کنند. این هدف سند برای مرحلهٔ اول ورود به بازار است (بخش ۱۷).
2. **تثبیت با بازخورد واقعی:** هر هفته مشکلات فروشنده‌های آزمایشی جمع و رفع شود (بخش ۱۷: «تیم باید نحوهٔ کار فروشندگان را مستقیماً بررسی کند»).
3. **پرداخت آنلاین سفارش‌ها:** سند درگاه پرداخت را «روش اصلی و استاندارد ثبت پرداخت» می‌داند (بخش ۹).
4. **اطلاع‌رسانی خودکار به مشتری با پیامک:** ثبت سفارش، تأیید پرداخت، ارسال و کد رهگیری (بخش ۱۰: «اطلاع‌رسانی به مشتری تا حد زیادی خودکار»).
5. **پایهٔ درآمد:** اشتراک ماهانه با پلن‌ها (بخش ۱۴)، اما **گرفتن پول فقط بعد از تأیید قیمت‌ها با فروشنده‌های آزمایشی**. سند صریحاً می‌گوید قیمت نهایی فقط بعد از مصاحبه با فروشندگان تعیین شود.
6. **اعتماد به داده:** پشتیبان‌گیری، خروجی گرفتن از داده‌ها و شفافیت (بخش ۲۰: «امنیت، پشتیبان‌گیری، کنترل دسترسی و شفافیت… باید از ابتدا جدی گرفته شود»).

## معیار موفقیت فاز ۲
- حداقل **۱۰ فروشگاه** هر هفته سفارش واقعی در سیستم ثبت کنند.
- مشکلات عملیاتی اصلی فروشنده‌ها با مصاحبهٔ هفتگی شناسایی و در Issueها ثبت شده باشند (سؤال‌های بخش ۱۷ سند).
- حداقل یک فروشنده سفارشی را با **پرداخت آنلاین** از ابتدا تا انتها گرفته باشد.
- قیمت پلن‌ها با فروشنده‌های آزمایشی سنجیده شده و تصمیم «روشن کردن دریافت اشتراک» گرفته شده باشد.

---

## دامنه
**P0** یعنی بدون آن فاز ۲ تمام نیست، **P1** یعنی مهم ولی در کمبود وقت قابل تعویق، **P2** یعنی اگر وقت ماند.

| کار | Track | اولویت |
|---|---|---|
| مرحلهٔ صفر: سرور، اسکیما، ذخیره‌سازی فایل، پیامک واقعی | A (B بازبین) | P0 |
| آزمایش سرتاسری مشترک (کار باقی‌ماندهٔ هفتهٔ ۵ فاز ۱) | هر دو | P0 |
| A6: تنظیمات فروشگاه (نام، لوگو، راه‌های تماس) | A | P0 |
| A7: انتقال عکس محصولات به ذخیره‌ساز S3 | A | P0 |
| A8: سرویس پیامک عمومی با ثبت و شمارش | A | P0 |
| A9: اشتراک و پلن‌ها (دریافت پول پشت flag) | A | P0 |
| A10: اعضای تیم (اپراتور) | A | P1 |
| A11: خروجی CSV محصولات و مشتریان، صفحهٔ حریم خصوصی | A | P1 |
| B6: پرداخت آنلاین سفارش با درگاه فروشنده | B | P0 |
| B7: پیامک خودکار به مشتری | B | P0 |
| B8: قیف لینک خرید (بازدید، سفارش، پرداخت) | B | P1 |
| B9: خروجی CSV سفارش‌ها و برگهٔ چاپ برای ارسال | B | P1 |
| B10: انتقال محدودکنندهٔ نرخ به دیتابیس | B | P2 (فقط اگر بیش از یک نمونه اجرا شود) |
| رفع مشکلات فروشنده‌های آزمایشی | هر دو | P0، حدود ۳۰٪ ظرفیت هر هفته |

## خارج از دامنهٔ فاز ۲ (برای فاز ۳)
پیام‌رسان‌ها (ربات تلگرام، صندوق پیام مشترک، اینستاگرام)، پاسخ خودکار و دستیار مدیریتی هوش مصنوعی (بخش‌های ۷، ۸ و ۱۲ سند)، ابزارهای بازاریابی، اتصال API شرکت‌های حمل‌ونقل، فروشگاه اینترنتی اختصاصی، تم تیره، و آماده‌سازی برای بازار خارجی.

---

## تصمیم‌های کلیدی
1. **پول مشتری مستقیم به حساب خود فروشنده برود، نه پلتفرم.** هر فروشنده درگاه خودش را در تنظیمات وصل می‌کند (مثلاً شناسهٔ پذیرندهٔ زرین‌پال).
   - **دلیل:** دریافت وجه به نیابت از کسب‌وکارهای دیگر در شبکهٔ پرداخت ایران مجوز خاص (پرداخت‌یار) می‌خواهد، و پول دیگران در حساب ما هم ریسک اعتماد است (بخش ۲۰ سند).
   - **پیش از B6** این تصمیم با یک PSP یا مشاور حقوقی تأیید شود.
   - پلتفرم فقط برای **اشتراک خودش** درگاه جدا دارد.
2. **کارت‌به‌کارت روش پیش‌فرض می‌ماند.** بسیاری از فروشنده‌های اینستاگرامی درگاه ندارند. درگاه یک گزینهٔ اضافه است، و حالا شمارهٔ کارت فروشنده روی صفحهٔ مشتری نمایش داده می‌شود. این همان کمبود ثبت‌شده در B3 است.
3. **محدودیت پلن هرگز سفارش مشتری را متوقف نمی‌کند.** اگر فروشنده از سقف پلن گذشت، به او هشدار داده می‌شود و کارهای سمت فروشنده (مثلاً افزودن عضو یا پیامک اضافه) محدود می‌شود. خرید مشتری هیچ‌وقت رد نمی‌شود.
4. **دریافت هزینهٔ اشتراک پشت `BILLING_ENABLED=false` می‌ماند** تا قیمت‌ها با فروشنده‌های آزمایشی تأیید شوند (بخش ۱۴). پلن‌ها، دورهٔ آزمایشی و شمارش مصرف از روز اول کار می‌کنند.
5. **فقط سرویس‌های داخلی، بدون وابستگی خارجی در زمان اجرا** (قانون ۷ فاز ۱ و بخش ۲۰ سند): میزبانی، دیتابیس و ذخیره‌ساز در لیارا یا آروان‌کلود، پیامک با کاوه‌نگار، درگاه‌های داخلی.

---

## مرحلهٔ صفر فاز ۲ (هفتهٔ ۰)
مالک: Person A، بازبین: Person B. کارهای هفتهٔ ۱ بعد از مرج این مرحله شروع می‌شوند. جزئیات در بخش «مرحلهٔ صفر» [TRACK-A.md](./TRACK-A.md) است. در همین هفته Person B آزمایش سرتاسری مشترک و آماده‌سازی فروشنده‌های آزمایشی را پیش می‌برد.

- [ ] اسکیمای کامل فاز ۲ در **یک** مایگریشن (جدول زیر)
- [ ] محیط پروداکشن: سرور داخلی، Postgres مدیریت‌شده، HTTPS، پراکسی معکوس با `TRUSTED_PROXY_HOPS` درست، اجرای `prisma migrate deploy` هنگام انتشار
- [ ] پشتیبان‌گیری روزانهٔ دیتابیس **و یک بار بازیابی آزمایشی**
- [ ] ماژول ذخیره‌ساز S3 با دو بخش: عمومی برای عکس محصول و خصوصی برای رسید
- [ ] قالب‌های پیامک کاوه‌نگار ثبت و ارسال برای تأیید شوند (تأییدشان زمان می‌برد، پس از همین هفته): کد ورود، ثبت سفارش، تأیید پرداخت، ارسال
- [ ] `src/lib/crypto.ts` برای رمزکردن اطلاعات حساس (مثل اطلاعات درگاه فروشنده) با `SECRETS_KEY`
- [ ] مسیر عمومی `/pay` در `src/proxy.ts`، برای بازگشت از درگاه
- [ ] `.env.example` به‌روز، و CI بدون تغییر رفتار
- [ ] **انتشار نسخهٔ فاز ۱ روی پروداکشن** در پایان هفته، تا فروشنده‌های آزمایشی از هفتهٔ ۱ شروع کنند

### افزوده‌های اسکیما (همه در مرحلهٔ صفر)
| مدل | فیلد یا تغییر | برای |
|---|---|---|
| `Seller` | `logoUrl`، `contactPhone`، `instagram`، `telegram` | A6، نمایش روی صفحه‌های عمومی |
| `Seller` | `cardNumber`، `cardHolder`، `sheba` | B6، راهنمای کارت‌به‌کارت (کمبود ثبت‌شده در B3) |
| `Seller` | `smsOnOrderPlaced`، `smsOnPaid`، `smsOnShipped` (Boolean، پیش‌فرض true) | B7 |
| `SellerGateway` | جدید: `sellerId` یکتا، `provider` (`ZARINPAL`/`IDPAY`)، `credentialsEncrypted`، `isActive` | B6 |
| `PaymentMethod` | مقدار جدید `ONLINE` | B6 |
| `PaymentAttempt` | جدید: `sellerId`، `orderId?`، `invoiceId?`، `provider`، `amount` (تومان)، `authority` یکتا، `status` (`PENDING`/`VERIFIED`/`FAILED`/`CANCELED`)، `refId`، `cardPanMasked`، `verifiedAt` | B6، A9 |
| `Subscription` | جدید: `sellerId` یکتا، `plan` (`TRIAL`/`BASIC`/`GROWTH`/`PRO`)، `status` (`TRIALING`/`ACTIVE`/`PAST_DUE`/`CANCELED`)، `currentPeriodEnd` | A9 |
| `Invoice` | جدید: `sellerId`، `plan`، `amount`، `periodStart`، `periodEnd`، `status` (`OPEN`/`PAID`/`VOID`)، `paidAt` | A9 |
| `SmsMessage` | جدید: `sellerId?`، `to`، `kind`، `orderId?`، `status` (`SENT`/`FAILED`/`DEV`)، `providerId`، `createdAt`. یکتا روی `(orderId, kind)` تا پیامک هر رویداد سفارش فقط یک بار ارسال شود | A8، B7 |
| `User`، `Membership` | جدید: `User(mobile یکتا)`، `Membership(userId, sellerId, role: OWNER/OPERATOR)`. برای هر فروشندهٔ فعلی یک `User` و عضویت `OWNER` ساخته می‌شود | A10 |
| `LinkDailyView` | جدید: `purchaseLinkId`، `day`، `views`، یکتا روی `(purchaseLinkId, day)`. فقط شمارش، بدون IP | B8 |

جدول‌های `User` و `Membership` حتی اگر A10 عقب بیفتد در همین مرحله ساخته می‌شوند. درس فاز ۱ این بود که اسکیما یک‌جا نهایی شود، چون بعداً فقط یک PR اسکیما در هر زمان مجاز است.

---

## قراردادهای بین دو Track
مثل `adjustStock` در فاز ۱: امضا ثابت است، تا آماده شدن نسخهٔ واقعی یک استاب با همان امضا وجود دارد، و یک تست نوعی جلوی جدا شدن امضای استاب از نسخهٔ واقعی را می‌گیرد.

| قرارداد | سازنده | مصرف‌کننده | استفاده |
|---|---|---|---|
| `getPublicStoreProfile(sellerId)` در `src/server/store/` | A (A6) | B | نام، لوگو و راه‌های تماس فروشگاه روی `/buy` و `/buy/order` |
| `storage.put / get / delete` با بخش `public` و `private` در `src/server/storage/` | A (مرحلهٔ صفر) | B | انتقال رسیدها از دیسک به ذخیره‌ساز خصوصی |
| `sendSms({ sellerId, to, kind, tokens, orderId })` در `src/server/sms/` | A (A8) | B | پیامک‌های مشتری. ثبت در `SmsMessage` و شمارش برای پلن |
| `getPlanUsage(sellerId)` و `canUse(sellerId, feature)` در `src/server/billing/` | A (A9) | B | مثلاً پیامک اضافه بعد از سهمیه. **هیچ‌وقت برای رد سفارش مشتری نیست** (تصمیم ۳) |
| `PaymentGateway` (موجود در `payment.ts`) و پیاده‌سازی زرین‌پال در `src/server/payments/` | B (B6) | A | پرداخت اشتراک پلتفرم در A9، با درگاه خود پلتفرم |

## قوانین مالکیت
همهٔ قوانین فاز ۱ پابرجاست ([docs/phase1/README.md](../phase1/README.md#ownership-rules)): مالکیت پوشه، یک PR اسکیما در هر زمان، کار با برنچ و PR با CI سبز، محدود کردن همهٔ داده‌ها به فروشنده، مبلغ به تومانِ عدد صحیح، و هیچ وابستگی خارجی در زمان اجرا. سه نکتهٔ جدید:

1. **اطلاعات درگاه و شمارهٔ کارت حساس است:** فقط رمزشده ذخیره شود و هرگز در لاگ، خطا یا صفحهٔ عمومی کامل نمایش داده نشود. شمارهٔ کارت فقط روی صفحهٔ سفارش همان مشتری نشان داده می‌شود.
2. **هر تأیید پرداخت آنلاین فقط از `confirmPaymentInTx()` می‌گذرد** (قاعدهٔ B3)، آن هم بعد از `verify` موفق درگاه و **برابر بودن مبلغ** با مبلغ قابل پرداخت سفارش.
3. **کاری که مستقیم روی پروداکشن انجام می‌شود** (اجرای مایگریشن، تغییر متغیر محیطی، بازیابی پشتیبان) در یک Issue یا PR ثبت شود.

## فرایند آزمایش با فروشنده‌ها (هر هفته، هر دو نفر)
- **شروع:** در هفتهٔ ۰ حدود ۱۰ فروشگاه فعال انتخاب می‌شوند، ترجیحاً پوشاک، کیف، کفش و اکسسوری، که بازار اولیهٔ پیشنهادی سند است (بخش ۱۳). برای هرکدام یک جلسهٔ راه‌اندازی ۳۰ دقیقه‌ای گذاشته می‌شود.
- **مصاحبهٔ هفتگی کوتاه** با سؤال‌های بخش ۱۷ سند:
  - سفارش را چطور ثبت می‌کنید؟
  - موجودی را چطور کنترل می‌کنید؟
  - پرداخت را چطور تأیید می‌کنید؟
  - بیشترین وقت کجا می‌رود؟
  - بیشترین خطا کجاست؟
  - مشتری بیشتر چه می‌پرسد؟
- **هر مشکل یک Issue:** با برچسب `pilot`، شمارهٔ فروشنده (بدون اطلاعات شخصی)، و اولویت.
- **جلسهٔ هفتگی ۳۰ دقیقه‌ای دو نفره:** مرور Issueها، تقسیم بین دو Track بر اساس مالکیت پوشه، و کنار گذاشتن حدود ۳۰٪ ظرفیت هفتهٔ بعد برای آن‌ها.
- **سنجش قیمت** (هفته‌های ۳ تا ۵): پلن‌ها و قیمت‌های پیشنهادی سند به فروشنده‌ها نشان داده شود و تمایل به پرداخت ثبت شود. تصمیم روشن کردن `BILLING_ENABLED` در هفتهٔ ۶ گرفته می‌شود.

## جدول زمانی (حدود ۷ هفته)
| هفته | Person A | Person B |
|---|---|---|
| ۰ | مرحلهٔ صفر (B بازبین)، انتشار فاز ۱ روی پروداکشن | بازبینی مرحلهٔ صفر، آزمایش سرتاسری مشترک، آماده‌سازی فروشنده‌ها، بررسی sandbox زرین‌پال |
| ۱ | A6 تنظیمات فروشگاه، A7 انتقال عکس‌ها به S3 | B6 پرداخت آنلاین (sandbox)، انتقال رسیدها به ذخیره‌ساز خصوصی |
| ۲ | A8 سرویس پیامک | B6 تکمیل (درگاه واقعی با یک فروشندهٔ آزمایشی)، شروع B7 روی استاب `sendSms` |
| ۳ | A9 اشتراک و پلن‌ها | B7 تکمیل روی `sendSms` واقعی، B8 قیف لینک خرید |
| ۴ | A10 اعضای تیم | B9 خروجی و برگهٔ چاپ |
| ۵ | A11 خروجی و حریم خصوصی، رفع مشکلات آزمایشی | رفع مشکلات آزمایشی، B10 در صورت نیاز |
| ۶ | مشترک: آزمایش سرتاسری، بازبینی امنیتی، تصمیم قیمت و `BILLING_ENABLED`، تصمیم ادامه به فاز ۳ | |

## تعریف انجام (هر تسک)
- **همهٔ موارد فاز ۱:** روی دادهٔ نمونه و راست‌به‌چپ کار کند، همه‌چیز به فروشنده محدود باشد، CI سبز باشد، و منطق مالی و موجودی تست دیتابیس داشته باشد.
- **روی پروداکشن هم امتحان شده باشد**، نه فقط روی کامپیوتر خودتان، مخصوصاً درگاه و پیامک.
- **هر ویژگی که با پول سروکار دارد**، مثل پرداخت یا اشتراک، یک تست برای «تکرار نشدن» داشته باشد. مثلاً بازگشت دوبارهٔ مشتری از درگاه نباید پرداخت را دو بار ثبت کند.

## ریسک‌ها
| ریسک | راهکار |
|---|---|
| بیشتر فروشنده‌های کوچک درگاه اختصاصی ندارند | کارت‌به‌کارت با نمایش شمارهٔ کارت، روش اصلی می‌ماند. B6 اختیاری است |
| تأیید قالب پیامک در کاوه‌نگار طول می‌کشد | ثبت قالب‌ها از هفتهٔ ۰. تا تأیید، B7 روی استاب کار می‌کند |
| ناپایداری اینترنت و محدودیت سرویس‌های خارجی | فقط سرویس‌های داخلی، بدون وابستگی خارجی در زمان اجرا |
| پرداخت آنلاینی که بعد از انقضای سفارش تأیید شود | در B6 مشخص است: سفارشِ در حال پرداخت منقضی نمی‌شود، و اگر پرداخت دیر برسد، سفارش برای بررسی دستی فروشنده علامت می‌خورد (جزئیات در TRACK-B) |
| پیچیدگی بیش از حد (بخش ۲۰ سند) | اولویت‌های P0 و P1 و P2. A10 و B8 تا B10 قابل تعویق‌اند |
| امکان محافظت از برنچ `main` در گیت‌هاب نیست (مخزن خصوصی با حساب رایگان) | یا گیت‌هاب Pro روی حساب مالک، یا ادامهٔ قانون دستی: هیچ push مستقیمی به `main` نشود |
